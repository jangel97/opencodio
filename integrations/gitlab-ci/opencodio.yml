.opencodio:
  image: quay.io/jangel97/opencodio:v0.0.1
  variables:
    OPENCODIO_STREAM: "1"
    NO_COLOR: "1"
    OPENCODIO_EXTRA_ARGS: ""
  script:
    - entrypoint.sh -p "${OPENCODIO_PROMPT}" ${OPENCODIO_EXTRA_ARGS}
